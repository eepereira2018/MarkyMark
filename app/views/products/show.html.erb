<head>
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>

<div class="card" style="width: 18rem; margin-bottom:1rem; margin-top:1rem;">
<div class="card-body">
<h5 class="card-title"><%= @product.title %> </h5>
<%= image_tag @product.image, style: 'width:15rem;' %>
<p class= "card-text"><%= @product.cost %></p>
<p class= "card-text"><%= @product.description %></p>
  <a href="#" class="card-link"><%= link_to "Destroy", product_path(@product), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></a>
    <%= link_to "Edit", edit_product_path(@product), class: "card-link" %>
</div>
</div>


